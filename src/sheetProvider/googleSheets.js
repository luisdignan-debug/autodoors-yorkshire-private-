async function writeTracker() {
  throw new Error(
    "Google Sheets live writing is intentionally left as a provider module placeholder. Use the Excel workbook MVP first, or add authorised Google Sheets API access with least-privilege scopes."
  );
}

module.exports = { writeTracker };
